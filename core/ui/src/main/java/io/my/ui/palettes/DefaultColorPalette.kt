package io.my.ui.palettes

import io.my.ui.AppColors
import io.my.ui.DefaultColor
import io.my.ui.Teal200

internal val DefaultPalette = AppColors(
    backgroundPrimary = DefaultColor,
    backgroundSecondary = DefaultColor,
    contendPrimary = DefaultColor,
    contendSecondary = DefaultColor,
    contendTertiary = DefaultColor,
    contendAccentPrimary = DefaultColor,
    contendAccentSecondary = DefaultColor,
    contendAccentTertiary = DefaultColor,
    textPrimary = DefaultColor,
    textSecondary = DefaultColor,
    textTertiary = DefaultColor,
    indicatorContendError = DefaultColor,
    indicatorContendDone = DefaultColor,
    indicatorContendSuccess = DefaultColor,
    primaryButton = DefaultColor,
    bottomMenuBackground = DefaultColor,
    scrimer = DefaultColor,
    calendarPeriod = DefaultColor,
    buttonSecondary = DefaultColor,
    black = DefaultColor,
    textButton = DefaultColor,
    isDark = false,
)